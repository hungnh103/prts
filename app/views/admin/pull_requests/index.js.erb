$('.admin-pull-requests-stat').html('<%= j render "admin/pull_requests/stat" %>');
$('.admin-pull-requests-table').html('<%= j render "admin/pull_requests/pull_requests" %>');

// label_name = sessionStorage.getItem('label_name');
// status = sessionStorage.getItem('status');
// // alert(status);
// check_box = $('.pull-request-status-label label[role=button] input[type=' + label_name + ']');

// if(status == 'checked') {
//   check_box.prop('checked', true);
//   check_box.attr('checked', 'checked');
// } else {
//   check_box.prop('checked', false);
//   check_box.removeAttr('checked');
// }
// sessionStorage.clear();
